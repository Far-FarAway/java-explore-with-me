CREATE TABLE IF NOT EXISTS stats (
    stat_id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app VARCHAR(511),
    uri VARCHAR(511) NOT NULL,
    ip VARCHAR(63) NOT NULL,
    saw_date TIMESTAMP NOT NULL
);